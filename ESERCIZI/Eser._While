#include <stdio.h>

int main() {
    int numero;
    int somma=0; 

printf("Inserisci un numero (0 per terminare): ");
        scanf("%d", &numero);
        
    while (numero != 0) {
        
        if (numero > 0) {
            somma += numero;
        }
    
    printf("\n La Somma totale dei numeri positivi inseriti è: %d\n",somma);
    return 0;
}
